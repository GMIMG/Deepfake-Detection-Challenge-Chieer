{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "core-build",
            "type": "shell",
            "command": "gradle clean build -xtest",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}